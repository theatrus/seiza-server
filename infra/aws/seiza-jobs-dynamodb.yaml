AWSTemplateFormatVersion: '2010-09-09'
Description: Durable Seiza Server job store with bounded recovery and account-history indexes.

Resources:
  SeizaJobs:
    Type: AWS::DynamoDB::Table
    Properties:
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
        - AttributeName: status
          AttributeType: S
        - AttributeName: owner
          AttributeType: S
        - AttributeName: created_at
          AttributeType: S
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: job-status-created-at
          KeySchema:
            - AttributeName: status
              KeyType: HASH
            - AttributeName: created_at
              KeyType: RANGE
          Projection:
            ProjectionType: INCLUDE
            NonKeyAttributes:
              - notification_delivered_at
        - IndexName: job-owner-created-at
          KeySchema:
            - AttributeName: owner
              KeyType: HASH
            - AttributeName: created_at
              KeyType: RANGE
          Projection:
            ProjectionType: INCLUDE
            NonKeyAttributes:
              - id
              - status
              - original_filename
              - started_at
              - completed_at
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true

Outputs:
  TableName:
    Value: !Ref SeizaJobs
  TableArn:
    Value: !GetAtt SeizaJobs.Arn
